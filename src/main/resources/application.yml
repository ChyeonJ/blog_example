spring:
  datasource:
    url: jdbc:mysql://localhost:3306/blog_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&useSSL=false
    username: blog_manager
    password: study
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto:  update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

# 이슈 발급자(issuer), 비밀키(secret_key) 필수 설정
jwt:
  issuer: chyeonj@gamail.com
  secret_key: study-springboot

